title: Return to Normalcy
subtitle: Have we finally found our way out of the crisis?
new-page: true
view-if: ((year = 1933 and month > 7) or (year >= 1934)) and pro_republic >= 50 and disfavored <= 13 and inflation < 7 and chancellor != "Goodryke" and chancellor != "Staal" and coup_progress < 9
max-visits: 1
on-arrival: return_to_normalcy = 1; commons_royalist *= 0.7 if not royalist_peak_triggered or royalist_votes >= 24; burgher_royalist *= 0.7 if not royalist_peak_triggered or royalist_votes >= 24; guild_royalist *= 0.7 if not royalist_peak_triggered or royalist_votes >= 24; landed_royalist *= 0.7 if not royalist_peak_triggered or royalist_votes >= 24; disfavored_royalist *= 0.7 if not royalist_peak_triggered or royalist_votes >= 24; sa_strength *= 0.7 if not royalist_peak_triggered or royalist_votes >= 24; sa_militancy -= 0.1 if not royalist_peak_triggered or royalist_votes >= 24; coup_progress -= 4 if coup_progress > 4; burgher_ddp += 5 if not royalist_peak_triggered or royalist_votes >= 24; burgher_gold += 3 if not royalist_peak_triggered or royalist_votes >= 24; guild_ddp += 3 if not royalist_peak_triggered or royalist_votes >= 24; guild_gold += 5 if not royalist_peak_triggered or royalist_votes >= 24; guild_traditionalist += 3; landed_traditionalist += 6; landed_ddp += 2 if not royalist_peak_triggered or royalist_votes >= 24; landed_gold += 2 if not royalist_peak_triggered or royalist_votes >= 24; burgher_lvp += 8; guild_lvp += 8; landed_lvp += 5; faithful_royalist *= 0.7 if not royalist_peak_triggered or royalist_votes >= 24; disfavored_collectivist += 3; disfavored_marcher += 3; strife -= 2; ddp_cohesion += 5; ddp_left += 6; gold_left += 12; lvp_left += 6
tags: event

= Return to Normalcy

the Commonwealth is finally recovering from the Great Depression. As the economy gradually improves, the political crisis begins to unravel as well.

Finally, the internal contradictions of the <span style="color: #7A3C00;">**Royalists**</span> have caught up to it. With the improving economic conditions, many business leaders are less interested in supporting them. Tensions between their remaining big capitalist funders and their lower middle class frontliners have come to a head, leading to desertions and public spats. This all saps Gallax's aura of strength, weakening <span style="color: #7A3C00;">Royalist</span> support across a variety of demographics.

- @celebrate: This is an occasion to celebrate.
- @complacency: The <span style="color: #7A3C00;">Royalists</span> are still a threat. We must not be too complacent.

@celebrate

This is an occasion to celebrate! Hopefully he <span style="color: #7A3C00;">Royalists</span> will no longer be a threat to the Republic.

@complacency

Even in their weakened state, the <span style="color: #7A3C00;">Royalists</span> are still a threat. We must not be too complacent.
